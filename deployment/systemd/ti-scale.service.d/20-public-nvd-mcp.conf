[Service]
# PID 1 copies the same root-owned source token into Ti-Scale's private,
# read-only credential mount. The application reads only this credential ID;
# no bearer value is placed in an environment variable or unit directive.
LoadCredential=public-nvd-mcp-token:/etc/ti-scale-mcp-nvd/mcp-token
